import { FC } from 'react';

const RangePlaceHolder: FC = () => {
  return (
    <div className="py-2 px-3 xs:text-sm text-xs leading-tight text-gray-500 bg-white border border-gray-300 hover:bg-gray-100 hover:text-gray-700">
      ...
    </div>
  );
};

export default RangePlaceHolder;
